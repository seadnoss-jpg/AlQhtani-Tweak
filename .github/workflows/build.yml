name: Test
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Hello
      run: echo "GitHub Actions works!"
    - name: Checkout
      uses: actions/checkout@v4
    - name: List files
      run: ls -la
